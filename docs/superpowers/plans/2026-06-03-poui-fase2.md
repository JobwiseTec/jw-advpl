# POUI Fase 2 — A Ponte REST (Angular ↔ Protheus) — Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Extrair as chamadas REST do frontend Angular (`this.http.get/post/...('/rota')`) e **cruzá-las com as rotas REST do Protheus** (`@Get('/rota')` / `WSMETHOD`, já indexadas em `rest_endpoints`) — entregando rastreabilidade ponta-a-ponta front↔back: `plugadvpl poui-bridge`.

**Architecture:** Recalibrado contra um projeto POUI real (full-stack). Achado-chave: o projeto real **não usa `<po-*>` tags nem PoPageDynamic** — usa **HttpClient puro** num service Angular contra um backend `.tlpp` com **tlppCore `@Get`/`@Post`** (rotas `/pedidos`, `/processar`, `/filiais`). O lado TLPP **já é extraído** pelo `extract_rest_endpoints` → tabela `rest_endpoints(verbo, path)`. Esta fase adiciona só o lado Angular + o join por path. (Parsing de `<po-*>`/PoPageDynamic move pra Fase 3, junto do catálogo/lint.)

**Tech Stack:** Python regex strip-first (sem AST TS), SQLite, Typer, pytest. Sem deps novas.

**Spec:** [docs/poui-pesquisa-e-plano.md](../../poui-pesquisa-e-plano.md) §1.4, §2.7. Depende da Fase 1 (já em main: `poui_projetos`, `ingest_poui`).

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `cli/plugadvpl/parsing/poui.py` *(mod)* | + `extract_angular_http_calls(content) -> list[dict]` (verbo, url, path_norm, linha). Puro. |
| `cli/plugadvpl/migrations/023_poui_datasources.sql` *(novo)* | Tabela `poui_datasources`. |
| `cli/plugadvpl/db.py` *(mod)* | `SCHEMA_VERSION` "22" → "23". |
| `cli/plugadvpl/ingest_poui.py` *(mod)* | Varre `.ts` do projeto, extrai http calls, persiste em `poui_datasources`. |
| `cli/plugadvpl/query.py` *(mod)* | `poui_bridge(conn)` — join `poui_datasources.path_norm` ↔ `rest_endpoints.path`. |
| `cli/plugadvpl/cli.py` *(mod)* | Comando `poui-bridge`; `ingest-poui` passa a extrair datasources também. |
| `skills/poui-bridge/SKILL.md` *(novo)* | Wrapper. |
| `cli/tests/unit/parsing/test_poui.py` *(mod)*, `cli/tests/unit/test_ingest_poui.py` *(mod)* | Testes. |
| `docs/cli-reference.md`, `docs/schema.md`, `CHANGELOG.md` *(mod)* | Docs. |

---

## Chunk 1: Parser + Schema + Ingest + Bridge + CLI

### Task 1: Parser — extrair chamadas HttpClient do Angular

**Files:**
- Modify: `cli/plugadvpl/parsing/poui.py`
- Test: `cli/tests/unit/parsing/test_poui.py`

- [ ] **Step 1: Write the failing test**

```python
# adicionar em cli/tests/unit/parsing/test_poui.py
from plugadvpl.parsing.poui import extract_angular_http_calls


def test_http_call_literal_e_template() -> None:
    ts = """
    getPedidos() { return this.http.get<Pedido[]>('/pedidos'); }
    salvar(p) { return this.http.post(`${environment.api}/processar`, p); }
    del(id) { return this.http.delete(`${env.base}/pedidos/${id}`); }
    """
    calls = extract_angular_http_calls(ts)
    verbos = {(c["verbo"], c["path_norm"]) for c in calls}
    assert ("GET", "/pedidos") in verbos
    assert ("POST", "/processar") in verbos
    assert ("DELETE", "/pedidos") in verbos  # ${id} dinâmico é descartado do path_norm


def test_http_call_ignora_nao_http() -> None:
    assert extract_angular_http_calls("foo.get('/x'); array.post(1)") == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cli && uv run pytest tests/unit/parsing/test_poui.py -k http -q --override-ini="addopts="`
Expected: FAIL — `cannot import name 'extract_angular_http_calls'`

- [ ] **Step 3: Write minimal implementation**

Adicionar em `cli/plugadvpl/parsing/poui.py`:

```python
# Chamada HttpClient Angular: this.http.get<T>('url') / http.post(`...`) etc.
# Captura verbo + 1º argumento string/template literal.
_HTTP_CALL_RE = re.compile(
    r"\bhttp\s*\.\s*(get|post|put|delete|patch)\s*(?:<[^>]*>)?\s*\(\s*([`'\"])(.*?)\2",
    re.IGNORECASE | re.DOTALL,
)
# Segmento de path "/palavra" — ignora interpolação ${...} e querystring.
_PATH_SEG_RE = re.compile(r"/[A-Za-z][\w-]*")


def _path_norm(url: str) -> str:
    """Normaliza a URL pro path casável com rest_endpoints.path.

    Tira interpolação `${...}`, host e querystring; devolve os segmentos
    estáticos `/a/b` (o último segmento dinâmico tipo `/${id}` some)."""
    sem_interp = re.sub(r"\$\{[^}]*\}", "", url)
    segs = _PATH_SEG_RE.findall(sem_interp)
    return "".join(segs) if segs else ""


def extract_angular_http_calls(content: str) -> list[dict[str, object]]:
    """Chamadas REST HttpClient num fonte Angular .ts. Funções puras."""
    out: list[dict[str, object]] = []
    for m in _HTTP_CALL_RE.finditer(content):
        verbo = m.group(1).upper()
        url = m.group(3)
        path = _path_norm(url)
        if not path:
            continue
        linha = content.count("\n", 0, m.start()) + 1
        out.append({"verbo": verbo, "url": url, "path_norm": path, "linha": linha})
    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd cli && uv run pytest tests/unit/parsing/test_poui.py -k http -q --override-ini="addopts="`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add cli/plugadvpl/parsing/poui.py cli/tests/unit/parsing/test_poui.py
git commit -m "feat(poui): extrai chamadas HttpClient do Angular (verbo+path)"
```

---

### Task 2: Migration 023 + schema bump

**Files:**
- Create: `cli/plugadvpl/migrations/023_poui_datasources.sql`
- Modify: `cli/plugadvpl/db.py` (`SCHEMA_VERSION` "22"→"23")
- Test: `cli/tests/unit/test_ingest_poui.py`

- [ ] **Step 1: Write the failing test**

```python
def test_tabela_poui_datasources_existe(conn: sqlite3.Connection) -> None:
    cols = {r[1] for r in conn.execute("PRAGMA table_info(poui_datasources)")}
    assert {"caminho", "linha", "verbo", "path_norm", "url_raw"} <= cols
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cli && uv run pytest tests/unit/test_ingest_poui.py::test_tabela_poui_datasources_existe -q --override-ini="addopts="`
Expected: FAIL — `no such table: poui_datasources`

- [ ] **Step 3: Write migration + bump**

```sql
-- cli/plugadvpl/migrations/023_poui_datasources.sql
-- Migration 023 — datasources REST do frontend POUI (chamadas HttpClient).
-- Cruzadas com rest_endpoints (backend TLPP) via poui_bridge.
CREATE TABLE IF NOT EXISTS poui_datasources (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    caminho     TEXT NOT NULL,           -- .ts onde a chamada está (absoluto)
    linha       INTEGER NOT NULL DEFAULT 0,
    verbo       TEXT NOT NULL,           -- GET|POST|PUT|DELETE|PATCH
    url_raw     TEXT NOT NULL DEFAULT '',
    path_norm   TEXT NOT NULL DEFAULT ''  -- path estático casável com rest_endpoints.path
);
CREATE INDEX IF NOT EXISTS idx_poui_ds_path ON poui_datasources(path_norm);
```

`cli/plugadvpl/db.py`: `SCHEMA_VERSION = "23"`.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd cli && uv run pytest tests/unit/test_ingest_poui.py -q --override-ini="addopts=" && uv run pytest tests/unit/test_db.py -q --override-ini="addopts="`
Expected: PASS (tabela existe + test_db confirma v23↔migration 023)

- [ ] **Step 5: Commit**

```bash
git add cli/plugadvpl/migrations/023_poui_datasources.sql cli/plugadvpl/db.py cli/tests/unit/test_ingest_poui.py
git commit -m "feat(poui): migration 023 tabela poui_datasources (schema v23)"
```

---

### Task 3: ingest — varrer .ts e persistir datasources

**Files:**
- Modify: `cli/plugadvpl/ingest_poui.py`
- Test: `cli/tests/unit/test_ingest_poui.py`

- [ ] **Step 1: Write the failing test**

```python
def test_ingest_extrai_datasources(conn: sqlite3.Connection, tmp_path: Path) -> None:
    from plugadvpl.ingest_poui import ingest_poui_dir

    proj = tmp_path / "front"
    proj.mkdir()
    (proj / "package.json").write_text(
        '{"dependencies": {"@po-ui/ng-components": "15.0.0"}}', encoding="utf-8"
    )
    svc = proj / "src" / "app" / "services"
    svc.mkdir(parents=True)
    (svc / "pedido.service.ts").write_text(
        "getAll(){return this.http.get<X[]>('/pedidos');}", encoding="utf-8"
    )
    ingest_poui_dir(conn, tmp_path)
    rows = conn.execute(
        "SELECT verbo, path_norm FROM poui_datasources"
    ).fetchall()
    assert ("GET", "/pedidos") in rows
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cli && uv run pytest tests/unit/test_ingest_poui.py::test_ingest_extrai_datasources -q --override-ini="addopts="`
Expected: FAIL — 0 rows (ingest ainda não varre .ts)

- [ ] **Step 3: Write implementation**

Em `ingest_poui.py`: após persistir o projeto (quando `proj is not None`), varrer os `.ts` **abaixo do diretório do package.json** e extrair http calls. Adicionar import `from plugadvpl.parsing.poui import extract_angular_http_calls` e uma função:

```python
def _ingest_datasources(conn: sqlite3.Connection, proj_root: Path) -> None:
    """Varre .ts do projeto, extrai HttpClient calls -> poui_datasources.
    Limpa as do projeto antes (rebuild atômico por projeto)."""
    proj_abs = str(proj_root.resolve())
    conn.execute("DELETE FROM poui_datasources WHERE caminho LIKE ?", (proj_abs + "%",))
    for ts in proj_root.rglob("*.ts"):
        if any(part in _SKIP_DIRS for part in ts.relative_to(proj_root).parts):
            continue
        try:
            txt = ts.read_text(encoding="utf-8", errors="replace")
        except OSError:
            continue
        for call in extract_angular_http_calls(txt):
            conn.execute(
                "INSERT INTO poui_datasources (caminho, linha, verbo, url_raw, path_norm) "
                "VALUES (?, ?, ?, ?, ?)",
                (str(ts.resolve()), call["linha"], call["verbo"], call["url"], call["path_norm"]),
            )
```

Chamar `_ingest_datasources(conn, pkg_path.parent)` dentro do loop de `ingest_poui_dir`, logo após o `INSERT ... poui_projetos`, quando `proj is not None`. (Manter o `conn.commit()` final.)

- [ ] **Step 4: Run test to verify it passes**

Run: `cd cli && uv run pytest tests/unit/test_ingest_poui.py -q --override-ini="addopts="`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add cli/plugadvpl/ingest_poui.py cli/tests/unit/test_ingest_poui.py
git commit -m "feat(poui): ingest extrai datasources REST dos .ts do projeto"
```

---

### Task 4: query — `poui_bridge` (cross-link front↔back)

**Files:**
- Modify: `cli/plugadvpl/query.py`
- Test: `cli/tests/unit/test_ingest_poui.py`

- [ ] **Step 1: Write the failing test**

```python
def test_poui_bridge_casa_front_e_back(conn: sqlite3.Connection) -> None:
    from plugadvpl.query import poui_bridge

    # back (rest_endpoints já existe no schema): rota TLPP @Get /pedidos
    conn.execute(
        "INSERT INTO rest_endpoints (arquivo, classe, funcao, verbo, path, annotation_style) "
        "VALUES ('PEDREST.tlpp', '', 'getPedidos', 'GET', '/pedidos', '@verb_tlpp')"
    )
    # front: chamada Angular casável
    conn.execute(
        "INSERT INTO poui_datasources (caminho, linha, verbo, url_raw, path_norm) "
        "VALUES ('src/app/services/pedido.service.ts', 10, 'GET', '/pedidos', '/pedidos')"
    )
    conn.commit()
    rows = poui_bridge(conn)
    assert len(rows) == 1
    b = rows[0]
    assert b["verbo"] == "GET"
    assert b["path"] == "/pedidos"
    assert b["back_arquivo"] == "PEDREST.tlpp"
    assert "pedido.service.ts" in b["front_arquivo"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cli && uv run pytest tests/unit/test_ingest_poui.py::test_poui_bridge_casa_front_e_back -q --override-ini="addopts="`
Expected: FAIL — `cannot import name 'poui_bridge'`

- [ ] **Step 3: Write implementation**

Em `query.py`:

```python
def poui_bridge(conn: sqlite3.Connection) -> list[dict[str, Any]]:
    """Cruza datasources POUI (front) com rest_endpoints (back TLPP) por verbo+path.

    Casa quando o path do backend é igual ao path_norm do front, OU o path_norm
    do front TERMINA com o path do backend (front costuma ter prefixo de base)."""
    rows = conn.execute(
        """
        SELECT ds.caminho, ds.linha, ds.verbo, ds.path_norm,
               re.arquivo, re.funcao, re.path
        FROM poui_datasources ds
        JOIN rest_endpoints re
          ON UPPER(ds.verbo) = UPPER(re.verbo)
         AND re.path != ''
         AND (ds.path_norm = re.path OR ds.path_norm LIKE '%' || re.path)
        ORDER BY ds.caminho, ds.linha
        """
    ).fetchall()
    return [
        {
            "front_arquivo": c, "front_linha": ln, "verbo": v, "path": p,
            "back_arquivo": ra, "back_funcao": rf,
        }
        for c, ln, v, p, ra, rf, rp in rows
    ]
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd cli && uv run pytest tests/unit/test_ingest_poui.py -q --override-ini="addopts="`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add cli/plugadvpl/query.py cli/tests/unit/test_ingest_poui.py
git commit -m "feat(poui): query poui_bridge cruza datasource front x rest_endpoints back"
```

---

### Task 5: CLI — comando `poui-bridge`

**Files:**
- Modify: `cli/plugadvpl/cli.py`
- Test: `cli/tests/integration/test_cli.py`

- [ ] **Step 1: Write the failing test**

```python
class TestPouiBridge:
    def test_bridge_lista_match(self, runner: CliRunner, tmp_path: Path) -> None:
        # Front: projeto POUI com 1 service chamando /pedidos
        proj = tmp_path / "front"
        (proj / "src").mkdir(parents=True)
        (proj / "package.json").write_text(
            '{"dependencies": {"@po-ui/ng-components": "15.0.0"}}', encoding="utf-8"
        )
        (proj / "src" / "p.service.ts").write_text(
            "f(){return this.http.get('/pedidos');}", encoding="utf-8"
        )
        # Back: fonte TLPP com @Get /pedidos
        (tmp_path / "PEDREST.tlpp").write_text(
            '@Get("/pedidos")\nMethod getPed() Class P\nReturn\nEndMethod\n', encoding="utf-8"
        )
        # ingere os dois lados no MESMO índice, depois cruza
        runner.invoke(app, ["--root", str(tmp_path), "ingest-poui", str(proj)])
        runner.invoke(app, ["--root", str(tmp_path), "ingest", str(tmp_path)])
        result = runner.invoke(app, ["--root", str(tmp_path), "poui-bridge"])
        assert result.exit_code == 0
        out = (result.stderr or "") + result.stdout
        assert "/pedidos" in out and "PEDREST" in out
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd cli && uv run pytest tests/integration/test_cli.py::TestPouiBridge -q --override-ini="addopts="`
Expected: FAIL — `No such command 'poui-bridge'`

- [ ] **Step 3: Write implementation**

Em `cli.py` — import `from plugadvpl.query import poui_bridge as q_poui_bridge`. Comando (RO, modelar no padrão de query do `ingest-sx`/comandos de leitura — usar `ctx.obj["db"]`, `_render_from_ctx`):

```python
@app.command(name="poui-bridge")
def poui_bridge(ctx: typer.Context) -> None:
    """Cruza datasources POUI (front Angular) com rotas REST do Protheus (back TLPP)."""
    db_path: Path = ctx.obj["db"]
    conn = open_db(db_path)
    try:
        apply_migrations(conn)
        rows = q_poui_bridge(conn)
    finally:
        close_db(conn)
    linhas = [
        {
            "verbo": r["verbo"], "path": r["path"],
            "front": f"{r['front_arquivo']}:{r['front_linha']}",
            "back": f"{r['back_arquivo']} ({r['back_funcao']})",
        }
        for r in rows
    ]
    _render_from_ctx(ctx, linhas, columns=["verbo", "path", "front", "back"], title="POUI ↔ Protheus (ponte REST)")
```

> **Nota:** confirme `_render_from_ctx`/`ctx.obj` lendo `ingest-poui` (Fase 1, já em cli.py) — reuse o MESMO padrão.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd cli && uv run pytest tests/integration/test_cli.py::TestPouiBridge -q --override-ini="addopts="`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add cli/plugadvpl/cli.py cli/tests/integration/test_cli.py
git commit -m "feat(poui): subcommand poui-bridge (front Angular x back Protheus)"
```

---

### Task 6: Skill + docs + lint final

**Files:**
- Create: `skills/poui-bridge/SKILL.md` (template de `skills/ingest-poui/`)
- Modify: `docs/cli-reference.md`, `docs/schema.md`, `CHANGELOG.md`, `.github/workflows/ci.yml` (já tem poui.py/ingest_poui.py no LINT_FILES — nada novo a adicionar)
- Se a nova skill bumpar a contagem (58→59), atualizar `_skill_catalog.py` + os testes `test_has_58_skills` etc. (como na Fase 1).

- [ ] **Step 1:** criar skill `poui-bridge` (pin `uvx plugadvpl@<versão atual do plugin.json>`), atualizar docs + CHANGELOG `[Unreleased]`, ajustar contagem de skills se preciso.

- [ ] **Step 2: Verificação completa**

```bash
cd cli
uv run ruff check plugadvpl/parsing/poui.py plugadvpl/ingest_poui.py plugadvpl/query.py plugadvpl/cli.py
uv run ruff format --check plugadvpl/parsing/poui.py plugadvpl/ingest_poui.py
uv run mypy plugadvpl/parsing/poui.py plugadvpl/ingest_poui.py
uv run pytest tests/unit tests/integration -q --override-ini="addopts="
uv run python ../scripts/validate_plugin.py
```
Expected: tudo verde.

- [ ] **Step 3: Commit**

```bash
git add skills/poui-bridge/SKILL.md docs/ CHANGELOG.md cli/plugadvpl/_skill_catalog.py cli/tests/
git commit -m "feat(poui): skill poui-bridge + docs"
```

---

## Definition of Done (Fase 2)
- `plugadvpl ingest-poui <dir>` agora também extrai os datasources REST (HttpClient) dos `.ts`.
- `plugadvpl poui-bridge` lista os matches **front Angular ↔ back Protheus** (verbo+path), reusando `rest_endpoints` já indexado.
- Tabela `poui_datasources` (migration 023, schema v23); `query.poui_bridge`.
- Skill `poui-bridge`; docs + CHANGELOG; suite verde, ruff+mypy limpos.

## Validação real (pós-merge)
Rodar `ingest-poui` + `ingest` + `poui-bridge` contra o projeto full-stack real (inspeção local, **sem expor nada**): deve casar a chamada `http.get('/pedidos')` do service Angular com o `@Get('/pedidos')` do `.tlpp`.

## Não nesta fase (→ Fase 3)
Parsing de `<po-*>` tags + bindings `p-*` + catálogo `poui_componentes` + lint anti-alucinação.
